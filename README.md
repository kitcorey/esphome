This is a collection of esphom configurations that I use for devices in my home.

## First-time setup

1. `mise install` — installs Python, uv, gitleaks.
2. `mise run install-hooks` — wires `core.hooksPath` to `scripts/hooks`.
