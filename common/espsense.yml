external_components:
  - source: github://kitcorey/ESPSense@mac
    components: [espsense]

espsense:
  plugs:
    - name: "${name}"
      #current_sensor: current_sensor
      mac_address: "${espsense_mac_address}"
      power_sensor: power_sensor
      voltage_sensor: voltage_sensor
      voltage: 120.0
